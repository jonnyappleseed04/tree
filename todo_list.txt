notes: 252204
We can combine the python files in 'table_code' into etl.py
composite key for coordinates have duplicate entry
(showing error when I put into query)

object_id in  location type isn't a good foreign key for coordinates
because location_type is identified by tree
we probably have to change it so the tree has a many-to-one
relationship with location_type

to do:
generate synthetic data (fire_hydrant)
insert data into all tables
tables following Y indicates all data successfully transfered

coordinates
fire_hydrant (done)
location_type
location_type_width
neighborhood
neighborhood_address
tree
tree_details
tree_details_species
tree_is_next_to_fire_hydrant (working on)

plan:
output one sql file that inserts raw data

order of execution:
neighborhood.sql
neighborhood_address.sql
location_type.sql
coordinates